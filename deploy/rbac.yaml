apiVersion: v1
kind: ServiceAccount
metadata:
  name: capture-controller-sa
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: capture-controller-role
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: capture-controller-binding
subjects:
- kind: ServiceAccount
  name: capture-controller-sa
  namespace: default
roleRef:
  kind: ClusterRole
  name: capture-controller-role
  apiGroup: rbac.authorization.k8s.io
